# Android build outputs
/app/build/
/build/

# Gradle caches / wrapper data (not the wrapper scripts)
.gradle/
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Local properties (contains SDK path, do not commit)
local.properties

# IDE files
.idea/
*.iml
.DS_Store

# Signed keystores (never commit real keystores)
*.jks
*.keystore
